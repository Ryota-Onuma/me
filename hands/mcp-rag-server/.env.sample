# ローカル環境用の環境変数設定
# Docker を使用しない場合は、このファイルを .env にコピーして使用してください
# cp .env.sample .env

# PostgreSQL接続情報（ローカル環境用）
POSTGRES_HOST=localhost
POSTGRES_PORT=25432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=ragdb

# ドキュメントディレクトリ（ローカルパス）
SOURCE_DIR=./data/source
PROCESSED_DIR=./data/processed

# プロジェクト名（スコープ）
PROJECT=default

# エンベディングモデル設定
EMBEDDING_MODEL=intfloat/multilingual-e5-large
EMBEDDING_DIM=1024
EMBEDDING_PREFIX_QUERY="query: "
EMBEDDING_PREFIX_EMBEDDING="passage: "

# 他のエンベディングモデル例
# EMBEDDING_MODEL=cl-nagoya/ruri-v3-30m
# EMBEDDING_DIM=256
# EMBEDDING_PREFIX_QUERY="検索クエリ: "
# EMBEDDING_PREFIX_EMBEDDING="検索文書: "

# ログ設定（オプション）
ENABLE_FILE_LOGGING=false  # デフォルト: false（ログファイル出力なし）
